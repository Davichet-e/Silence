# Database configuration
DB_CONFIG = {
    "host": "localhost",
    "port": 3306,
    "user": "iissi_user",
    "password": "iissi$user",
    "database": "test",
    "db_engine": "mariadb",
}

# Supported DB engines are "oracle" and "mariadb" (MariaDB also supports MySQL)
